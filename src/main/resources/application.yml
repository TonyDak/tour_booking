

server:
    port: 8080
    servlet:
        context-path: /api
spring:
    profiles:
        active: dev
    datasource:
        url: jdbc:mysql://103.173.227.90:3306/tour-booking
        username: tour-booking
        password: n0XKVVma)hmE
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
    mail:
        host: ${MAIL_HOST}
        port: ${MAIL_PORT}
        username: ${MAIL_USERNAME}
        password: ${MAIL_PASSWORD}
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
    payment:
        vnPay:
            tmnCode: ${TMN_CODE}
            hashSecret: ${VNPAY_KEY}
            returnUrl: ${VNPAY_RETURN_URL}
            ipnUrl: ${VNPAY_URL}

    servlet:
      multipart:
          max-file-size: 10MB
          max-request-size: 10MB
jwt:
    secret: ${SECRET_KEY}

aws:
    s3:
        bucket-name: ${BUCKET_NAME}
        endpoint: ${ENDPOINT}
        access-key-id: ${ACCESS_KEY}
        secret-access-key: ${SECRET_KEY_AWS}
        region: ${REGION}
